#!/usr/bin/env bash

#This is the env variables template. Copy it locally and rename .default to .local and change settings as desired. .local files should not be synced into git.

# deployment_lib.sh:

# local database:
database=
host="/var/run/postgresql" # this is the default and can be overridden by supplying the -h flag
username=dbadmin
port=5432
env=dev # options: [ dev | prod ]

# if local is a dev environment:
#prod_host=
#prod_port=
#regen_docs=TRUE